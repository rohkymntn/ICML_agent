# Scripts not used by EditGuard

These CLIs are scaffolded for the future PhaseAgent submission and are not
wired into any EditGuard pipeline. They are kept here for reference; do not
include their outputs in EditGuard headline figures or tables.

- `train_survival_model.py` — currently raises SystemExit on invocation;
  trains the scaffolded `phaseagent._experimental.survival_model` once a
  curated combinatorial-survival target table exists.
