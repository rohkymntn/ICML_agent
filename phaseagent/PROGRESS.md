# PROGRESS — Learned Epistasis for Protein Function (ICML, story C)

Status legend: ☐ not started · ◐ in progress / pending artifact · ☑ done & verified

## Definition of Done

1. ◐ `paper/epistasis_icml.tex` exists, compiles cleanly with `tectonic` (verified iter19, **7 pp, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder in body**, official `icml2025` style committed), all standard sections present. — **ALL grounded result numbers are folded in (Model 1/2 iter13/14, zero-shot iter5/13, e2e iter13);** the earlier "PENDING: fold in numbers" note is STALE. Remaining ◐ ONLY for the generative-design subsection (the `gen_design.json` discrete-diffusion guidance baseline, still baking) — no fabricated numbers anywhere.
2. ☑ Headline cross-protein generalization number (e2e LoRA, ~25 held-out proteins) is a REAL number from a committed run artifact, in the paper. — **DONE (iter13):** `outputs/epistasis/e2e_results.json` committed (run `ap-ayiJz1iYjfF0M5qUt0Rhaj`), median per-held-out-protein Spearman **0.33** (mean 0.33, 80% of 25 held-out proteins above ρ>0.2, vs zero-shot reference 0.25) folded into paper §7.5 `tab:e2e` + abstract + contributions + CLAIMS.md; guarded by `tests/test_e2e_artifact.py`.
3. ☑ Model 1 (held-out doubles AND held-out positions) + Model 2 (gain-of-function recovery + matched-additive control Spearman) numbers, each traceable to committed CSV/parquet under `outputs/epistasis/`. — **DONE (iter13 + iter14).** **MODEL 1 (iter13):** both assays committed (`model1_oof_{GB1_Olson,GFP}.csv` + summaries) and folded into paper §7.3 `tab:model1` (GB1 doubles **0.36** / position **0.12**; GFP **0.14** / **0.08**), guarded by `tests/test_model1_committed.py`. **MODEL 2 (iter14):** `model2_oof_GB1.csv` + `model2_GB1_summary.json` committed (12,000 held-out GB1 doubles) and folded into paper §7.4 `tab:model2` + `fig:model2`: gain-of-function recovery (Model 1 lift **+1.09** over the additive pool mean, oracle +1.62) + the matched-additive within-bin Spearman **0.28** (10 bins), guarded by the now-active `tests/test_model2_committed.py`. — DECOMPOSITION part grounded (`outputs/epistasis/decomposition_{atlas.csv,summary.json}`, in paper Table); **ALL four function `.npz` now landed on the volume (iter8): `feat_{GB1_Olson,GFP}.npz` + `shift_{GB1_Olson,GFP}.npz` DONE** — DoD #3 Model 1 is now UNBLOCKED and is a pure-CPU local step (`train_model1.py` already writes the committed `model1_oof_<assay>.csv` + summary; guard `tests/test_model1_committed.py` in place, skips until they land). Next iteration: run `train_model1.py` to completion for both assays (~9 min/assay CPU), commit the OOF CSVs + summaries, fold the held-out-doubles / held-out-position Spearmans into a new paper Model 1 table + CLAIMS.md. **Model 2 gating step DONE (iter12):** `model2_design.py` refactored into `summarize_model2`/`run_model2`/`figure_model2`/`main` (no longer runs a 9-min job at import) — it now writes the committed `model2_oof_GB1.csv` + `model2_GB1_summary.json` (gain-of-function recovery + matched-additive within-bin Spearman), guarded by `tests/test_model2_committed.py`. **Model 2 run LAUNCHED DETACHED (iter14, `os.setsid`, PID 81468, PPID=1) — and the experiment was extended to also emit the DoD #4 best-of-N rerank baseline** (`gof_top10pct_zeroshot` / `gof_lift_zeroshot`, zero-shot DPLM PLL as the rerank reward), so the ONE run produces the complete Model 2 + design-comparator artifact. HARVEST next iteration: commit `model2_oof_GB1.csv` + `model2_GB1_summary.json` + `fig9`, fold into a new paper §7.4 `tab:model2` (additive pool mean / zero-shot rerank / Model 1 / oracle + `matched_additive_spearman`) + CLAIMS.md, re-run `pytest -q` (guard auto-activates), recompile.
4. ◐ Main-conference baseline set on the SAME tasks: zero-shot DPLM PLL, additive baseline, best-of-N rerank, one strong discrete-diffusion guidance baseline (+ DRAKES / SVDD / reward-guided refinement if a generative claim is made). — additive baseline defined in paper; **zero-shot DPLM PLL baseline (STABILITY) DONE & COMMITTED** (`outputs/epistasis/headroom_{summary.json,dplm.parquet}`, run `ap-a7hQxHVfIOYVuoil7e6Go7`): median per-protein Spearman 0.25, in paper §7.2 `tab:zeroshot` + CLAIMS.md. **zero-shot DPLM PLL on FUNCTION (GB1/GFP) — MEASURED iter8 directly from the landed `feat_*.npz`: GFP 0.005, GB1_Olson 0.021 — essentially zero, confirming function epistasis is invisible to the zero-shot PLL score (vs stability 0.25, the §7.2 contrast).** **NOW COMMITTED + IN PAPER (iter13): `model1_{GB1_Olson,GFP}_summary.json:zeroshot_spearman` = GB1 0.021, GFP 0.005, in §7.2 + `tab:model1` + CLAIMS.md.** **best-of-N rerank design comparator — GROUNDED (iter14):** committed in `model2_GB1_summary.json` (`gof_top10pct_zeroshot` **−3.84** / `gof_lift_zeroshot` **−0.22**) and in paper §7.4 `tab:model2` — reranking the additively-mediocre design pool by the zero-shot DPLM PLL reward (the standard PLM-as-reward best-of-N baseline) does WORSE than random pool selection, vs Model 1's **+1.09** lift. So the full SAME-task baseline set is now grounded: additive (pool mean −3.62), zero-shot DPLM PLL rerank (−0.22 lift), Model 1 (+1.09), oracle (+1.62). **DISCRETE-DIFFUSION GUIDANCE BASELINE — NOW BEING COMPUTED (iter15), not argued.** Rather than leave B1 as a contested scope call, iter15 LAUNCHED the actual baseline the DoD names: `train_generative_design` (`ap-BlbHQjQn930EN3cSt5zDpZ`, H100, detached, RUNNING; an initial A10G launch OOM'd, fixed by switching the decorator to `gpu="H100"`) — a function-weighted (reward-guided) LoRA fine-tune of DPLM-650M (a discrete-diffusion PLM) that GENERATES high-function multi-mutants via iterative masked-diffusion sampling, evaluated on the COMPLETE combinatorial **GB1_Wu (4 sites, 149,360 measured combos)** so every generated combo is measurable. This IS "one strong discrete-diffusion guidance baseline." HARVEST next iteration → commit `gen_design.json`, fold a generative-design subsection + CLAIMS.md row + guard test, which dissolves B1 (the baseline exists by computation). Verified healthy past the cache-hit build into the function body (`[gen] GB1_Wu: 4 editable positions [265,266,267,280], 149360 measured combos`) and surviving the local client kill.
5. ☑ Every paper figure regenerates from committed data via a `scripts/` script; figure files committed; no orphan/degenerate figures. — **DONE (iter16); strengthened to BYTE-REPRODUCIBLE (iter20):** the three paper-figure scripts now pass `metadata={"CreationDate": None}` to the PDF `savefig`, dropping the embedded timestamp, so regenerating a figure twice yields byte-identical PDFs (verified: md5-identical across two regens) — "regenerate-and-diff" now actually verifies a figure instead of always showing a spurious timestamp diff. The 5 committed figure PDFs were re-emitted timestamp-free this iteration. All 5 committed figures are cited AND regenerate from committed data: fig2, fig3 (decomposition) + fig4 (zero-shot headroom) via `scripts/build_epistasis_figures.py --from-committed` (from `three_layer_atlas.parquet` + `outputs/epistasis/headroom_dplm.parquet`); **fig8 (Model 1 predict) via `scripts/build_model1_figure.py` from the committed `model1_oof_{GB1_Olson,GFP}.csv` (rewritten iter16 to read the committed CSVs, with a self-check that the drawn numbers equal the committed summaries)**; fig9 (Model 2 design) via `scripts/model2_design.py::figure_model2` from `model2_oof_GB1.csv`. Added a `--from-committed` path to `build_epistasis_figures.py` so the paper-cited decomposition/headroom figures regenerate without `/tmp` intermediates. **Deleted the 5 genuine orphans** (fig1, fig5, fig6, fig7, fig7a — uncited, regenerated only from non-committed `/tmp` raw assay CSVs). No degenerate figures (fig8 panel A bars are 0.02/0.36 and 0.005/0.14; panel B is a real ρ=0.36 hexbin).
6. ☑ If a calibration claim is retained, it uses test-time-valid methodology (temp scaling fit-on-val/test-on-test, or cross-fit isotonic). — **VERIFIED (iter19): NO calibration claim is retained in the paper** (grep for `calibrat|temperature scal|ECE|conformal` over `epistasis_icml.tex` returns nothing; the only `isotonic` mentions are the per-protein monotonic global-epistasis link, which is reported only as a held-out residual, never as a by-construction val-ECE/calibration claim). DoD #6 is vacuously satisfied — nothing to fix.
7. ☑ `pytest -q` passes — 122 passed, 6 skipped (verified iter25; the +2 vs iter24 are `tests/test_decomposition_ci.py`, guarding the new decomposition bootstrap-CI artifact; the 6 skips are the pre-staged `tests/test_gen_design_committed.py` guard, which skips until `gen_design.json` lands then asserts structural validity + the generative claim — reward-guided library beats unconditioned DPLM / random under the top-library ceiling).
8. ◐ `CLAIMS.md` maps every quantitative claim → exact committed artifact file + column. — **AUDIT PASS DONE (iter17):** extracted every number in the compiled body and confirmed each traces to a committed-artifact key. Closed the gaps found: added CLAIMS rows for the §7.3 zero-shot-gain deltas (GB1 **+0.34** / GFP **+0.13** → `model1_*_summary.json:delta_doubles`) and the e2e method params (rank **64** → `e2e_results.json:lora_r`, **10** epochs → `:epochs`); removed the one unbacked contextual number (the §7.1 "$\sim 0.1$ kcal/mol / four times" measurement-noise claim, which had no artifact and was inconsistent with fig3's own illustrative band). **iter28 re-trace fix:** the §7.6 "$0.25$ zero-shot reference" was mapped to `e2e_results.json:zero_shot_reference`, which is a HARDCODED literal in the e2e code, not a measurement — re-traced it to the real measured `headroom_summary.json:median_per_protein_spearman` (0.250987→0.25 on 30 stability domains) and reworded the paper so the 0.25 is honestly the corpus-level zero-shot ceiling, NOT a per-held-out-protein control. Remaining ◐ only because the generative-design subsection (gen_design.json, still baking) will add new numbers to row when it lands.
9. ◐ Limitations section honestly states what is not done. — section written; **strengthened (iter19): now honestly states that the cross-protein transfer result (median Spearman 0.33) is demonstrated on the Megascale STABILITY corpus** (the only corpus with enough proteins for a held-out-protein split), not on function, and that an analogous held-out-protein test of FUNCTIONAL epistasis transfer needs more combinatorial functional assays than exist — flagged as future work in both §6/§7.6 and the limitations section. Remaining ◐ only to add the generative-baseline scope note when `gen_design.json` lands (the current "we do not make a generative claim" sentence is accurate for the current paper and updates on harvest).

When ALL true & verified: set this file to COMPLETE, commit, report `ICML_READY`.

## State of the world (iteration 1 audit)

- Story C code is committed: `src/phaseagent/epistasis_decomposition.py`, `scripts/train_model1.py`, `scripts/model2_design.py`, `modal_app_v2.py::{train_epistasis_e2e,train_generative_design}`.
- Figures (PNG/PDF) for fig1–fig9 are committed under `paper/figures_epistasis/`, BUT their underlying numeric artifacts are NOT committed and are NOT on the Modal volume (`outputs/epistasis/` does not exist on the volume). Only committed data artifact: `paper/figures_epistasis/three_layer_atlas.parquet`.
- No `paper/epistasis_icml.tex` yet.
- `tectonic` 0.16.9 installed locally (iter2) — DoD #1 can be compiled/verified once the .tex exists.
- The numbers quoted in `HANDOFF_EPISTASIS_2026.md` need to be RE-GENERATED into committed CSV/parquet artifacts under `outputs/epistasis/` to be paper-eligible (hard rule: every claim traces to a committed artifact).

## Log

- iter42 (2026-06-26): **Converted the documented `gen_design.json` harvest landmine into a pre-verified, ready-to-apply patch (`HARVEST_GEN_DESIGN.md`), so the sole-gate harvest is mechanical and cannot leave the paper self-contradictory, while the DoD #4 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD`: still **RUNNING / healthy** at `date`=05:23 PDT (`modal app list` ephemeral / **1 active task**; `gen_design.json` absent from the volume; ~1 h 29 min real wall since the ~03:54 launch), on track to land ≈08:30-09:00 PDT inside the 8 h timeout. Left baking, did NOT relaunch (not-yet-failed). **The unit:** iter41 documented in RUNS.md *prose* that folding the gen result in requires reconciling two passages that currently DENY benchmarking discrete-diffusion guidance (§7-baselines lines 304-309, §9-limitations lines 670-673), else the paper self-contradicts — but left it as guidance, not an applicable patch. This iteration read the actual writer (`modal_app_v2.py:3260-3270`, the 11 `gen_design.json` keys) and the actual current paper passages and wrote the COMPLETE patch: exact new `\subsection{A discrete-diffusion guidance baseline}` (`\label{sec:results-gen}`, inserted after `fig:model2`) with `{{key}}` slots → the 11 JSON keys; the exact §7-baselines + §9-limitations replacement strings that reconcile the contradiction (verified all four gen cites `gruver2023protein/nisonoff2025unlocking/wang2025drakes/li2024svdd` still resolve afterward → no orphans); the exact CLAIMS.md GROUNDED rows; a guard-failure contingency (if the directional claim doesn't hold, reframe honestly + relax the guard, never force a number); and the final self-contradiction grep. The harvest is now "paste + fill 7 numbers", removing the highest-severity harvest risk (the run may land mid-iteration, as e2e/Model1/Model2 did, forcing fold-in + recompile + verify in one pressured turn). `pytest -q`: **122 passed / 6 skipped** (live-tree green-verified this iteration). Only `HARVEST_GEN_DESIGN.md` (new) + RUNS.md + PROGRESS.md touched (no code/tex/figure change → no recompile needed; paper unchanged). **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) by applying `HARVEST_GEN_DESIGN.md` → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter41 (2026-06-26): **Audited the complete bootstrap-CI traceability/reproducibility/guard chain end-to-end as a connected whole (first time as a set — no defect) and documented the highest-severity `gen_design.json` harvest landmine, while the sole remaining DoD #4 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD`: still **RUNNING / healthy** at `date`=05:13 PDT (`modal app list` ephemeral / **1 active task**; `gen_design.json` absent from the volume; ~1 h 19 min real wall since the ~03:54 launch). `modal app logs --timestamps` returned an EMPTY recent window across 4 attempts — consistent with healthy mid-epoch-1 (only the 03:58 data-load print so far, now outside the recent window; epoch-1-end print expected ≈05:27 PDT; a dead/OOM run drops to 0 tasks, this shows 1 sustained active task), so on track to land ≈08:30-09:00 PDT inside the 8 h timeout. Left baking, did NOT relaunch (not-yet-failed). **CI-chain audit (no defect found):** every CI range in the paper (4 decomposition + 8 model1 + 4 model2) matches its committed `*_ci.json` bound to the rounding shown AND is mapped in CLAIMS.md to the exact `*_ci.json` key; all three `bootstrap_*_ci.py` read ONLY committed data (`three_layer_atlas.parquet` / `model1_oof_*.csv` / `model2_oof_GB1.csv`) with no-drift self-checks; all three `tests/test_*_ci.py` guard point-estimate no-drift + CI-brackets-point + significance (a proportionate cheap b=100 re-run, documented in their docstrings). So the CIs are submission-ready end-to-end (paper → CLAIMS → JSON → committed-data script → no-drift test). **Harvest landmine documented (the committable contribution):** the §7-baselines paragraph (~line 305, "Stronger generative comparators … are required only if a generative design claim is made") and the §9-limitations sentence (~line 670, "we do not … benchmark against discrete-diffusion guidance") both currently DENY benchmarking a discrete-diffusion guidance baseline; folding `gen_design.json` in without reconciling them leaves a self-contradiction in the final paper (a §7.x discrete-diffusion guidance result while §9 says there is none). Added a prominent **HARVEST RECONCILIATION** note at the top of RUNS.md's Notes section quoting both passages and the correct reconciliation (keep Model 2 non-generative; present the gen run as an additional baseline/comparator; drop the "therefore do not benchmark" clause), plus a "grep for remaining 'do not benchmark'/'required only if' wording before flipping PROGRESS to COMPLETE" final-audit step. No prior iteration flagged this. `pytest -q`: **122 passed / 6 skipped** (live-tree green-verified this iteration). Only RUNS.md + PROGRESS.md touched (no code/tex/figure change → no recompile needed; paper unchanged). **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection + CLAIMS row + the pre-staged guard auto-activates → RECONCILE the §7/§9 "do not benchmark discrete-diffusion guidance" passages (see RUNS.md HARVEST RECONCILIATION note) → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter40 (2026-06-26): **Fixed a genuine method-text-vs-code inconsistency on the Model 2 design pool — §5 misdescribed how the design pool is constructed, contradicting both the actual code and §7.5 — a distinct method-fidelity reviewer axis (does the method section honestly describe the computation it runs) while the sole remaining DoD #4 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=05:05 PDT (`modal app list` ephemeral / **1 active task**; `modal volume ls outputs/epistasis` shows only the seven prior artifacts, `gen_design.json` absent; ~1 h 11 min real wall since the ~03:54 launch, on track to land ≈08:30-09:00 PDT inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Defect found by cross-checking the §5 method description against `scripts/model2_design.py`:** the code builds the design pool as `base < np.median(base)` with `base = df["glob"]` (the additive/global prediction) — i.e. the **double mutants whose additive prediction is below the median** (the "additively-mediocre" half). §7.5 already describes it correctly ("those below the median additive prediction"), but **§5 said the pool is "GB1 double mutants whose two singles are individually mediocre"** — a *per-single* criterion that is NOT what the code computes (a double with one terrible and one good single can sit below the median additive prediction without both its singles being mediocre). A reviewer cross-checking method vs. code (or method §5 vs. results §7.5) would flag the mismatch. **Fix (presentation/precision-only, no number changed → no CLAIMS.md/test impact):** reworded §5 to "We form this pool from the GB1 double mutants whose additive prediction falls below the median -- the additively-mediocre half, where the additive model rates every candidate roughly equal and cannot tell a synergistic pair from an antagonistic one; only specific epistasis distinguishes them." — now faithful to `base < np.median(base)` and parallel to §7.5's wording (the global link is monotonic, so the below-median split by global == the below-median split by additive). `tectonic` recompiles cleanly (**9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; verified the reworded sentence + the matching §7.5 "below the median additive prediction" render via `pdftotext`); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure/test touched — only `paper/epistasis_icml.tex`). Compiled to a throwaway `_build_iter40/` then removed it; `git status` shows only `paper/epistasis_icml.tex` (+ this file + RUNS.md), no leaked build artifacts or log procs. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates → update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter39 (2026-06-26): **Fixed a genuine factual citation error in the bibliography — `hsu2022learning` ("Learning protein fitness models from evolutionary and assay-labeled data") was entered as `@inproceedings` at ICML, but that titled paper was published in *Nature Biotechnology* 40(7):1114-1122 (2022) — a distinct bibliography-correctness axis no prior audit caught (iter34 only verified the bib was *well-formed*, not factually correct on venue), while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:58 PDT (`modal app list` ephemeral / **1 active task**; `modal volume ls outputs/epistasis` shows only the seven prior artifacts, `gen_design.json` absent; timestamped logs show only the 03:58:13 PDT data-load print, mid-epoch-1; ~1 h real wall since the ~03:54 launch, on track to land ≈09:00 PDT inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Two verification passes first (both clean, no defect):** (1) ran a dictionary-based spell-check of the paper prose (stripped LaTeX/math/`\texttt`/cite args, checked every word against `/usr/share/dict/words` + a domain allowlist) — every unmatched token is a plural/possessive/domain term/hyphenated compound the minimal macOS wordlist lacks, **no genuine typo**; (2) re-traced the e2e numbers vs `e2e_results.json` (12.85→12.9M params, median 0.33487→0.33, 124 trained / 25 held-out) and confirmed CLAIMS.md maps every paper number to a committed artifact key. **Defect found by reading the bib against the literature (not just for well-formedness):** `hsu2022learning` used the *Nature Biotechnology* article title with an ICML `booktitle` — a venue mismatch (the ICML/RECOMB version of this work has a different title; the "Learning protein fitness models…" title is the journal article). **Web-verified** the correct venue (nature.com/articles/s41587-021-01146-5: *Nat. Biotechnol.* 40(7):1114-1122, 2022) before changing anything, then converted the entry to `@article{… journal={Nature Biotechnology}, volume={40}, number={7}, pages={1114--1122}, year={2022}}` (cite key unchanged, so the single `\citep{hsu2022learning}` in §Related-work still resolves). `tectonic` recompiles cleanly (**9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; the bbl now renders "Nature Biotechnology, 40(7):1114-1122, 2022" — verified); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure/test touched — only `paper/epistasis.bib`). Compiled to a throwaway `_build/` then removed it; `git status` shows only `paper/epistasis.bib` (+ this file + RUNS.md), no leaked build artifacts. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates → update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter38 (2026-06-26): **Visually inspected the rendered figures (a distinct axis no prior iteration touched — every prior figure pass checked regeneration/byte-reproducibility, never the actual pixels) and fixed two figure-internal text defects a reviewer reading the figures would see, while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:55 PDT (`modal app list` ephemeral / **1 active task**; `modal volume ls outputs/epistasis` shows only the seven prior artifacts, `gen_design.json` absent; ~1 h real wall since the ~03:54 launch, on track to land ≈09:00 PDT inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Verification (live tree, not asserted):** rendered fig4/fig8/fig9 to PNG at 200 dpi and inspected them — all three are publication-quality (no degenerate bars pinned at 0/1, no clipped/overlapping text, correct legends/axes/value labels); confirms DoD #5 "no degenerate figures" holds on the actual pixels, not just on regeneration. **Two defects found + fixed (figure-internal title text, a reviewer-facing axis):** (1) `fig8` panel A title read "Model 1 predicts **function** epistasis" — the paper body uses "functional epistasis"/"functional assays" everywhere, so corrected to "**functional** epistasis" (`scripts/build_model1_figure.py:68`); (2) `fig9` panel A title read "Recovering gain-of-function **additive misses**", an ungrammatical compound that does not parse — corrected to the grammatical, concise "**Gain-of-function the additive model misses**" (`scripts/model2_design.py:188`). A first attempt ("Recovering gain-of-function the additive model misses") was grammatical but **too long and collided with panel B's title** — re-rendered, caught the collision visually, and shortened to the current fit-checked title (≤ the original length that fit cleanly). **Regenerated both figures from committed data**, re-rendered + visually re-verified the corrected titles render cleanly with no collision, and confirmed both PDFs **byte-reproduce md5-identical** across two regens (DoD #5 byte-reproducibility preserved: fig8 `6adbdc…`, fig9 `a85c49…`). The `.tex` is untouched (titles live in the figure scripts, not the paper), so all `\includegraphics` widths/refs are unchanged. Also verified two code/artifact-grounding claims while reading the figures (both clean, no change): the §6 LoRA method claim "adapters on the attention query/value projections" (rank 64) matches `modal_app_v2.py:3051-3052` (`target_modules=["query","value"]`, `lora_r=64`) exactly, and the §7.1 "85% of proteins exceed 0.3 kcal/mol" claim is backed by `decomposition_summary.json:frac_proteins_spec_std_gt_0p3`=0.852 with a CLAIMS.md row already present (line 24). `pytest -q`: **122 passed / 6 skipped**; paper recompiles **exit 0, 9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`**; `git status` shows only the 2 figure scripts + 4 regenerated figure files (+ this file + RUNS.md), no leaked build artifacts. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates → update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter37 (2026-06-26): **Fixed a latent harvest-blocker in the pre-staged DoD #4 guard before it could turn the tree red on landing: `tests/test_gen_design_committed.py` hardcoded `assert s["epochs"] == 5`, but iter31 stopped the doomed 5-epoch run and relaunched a 3-epoch replacement (`ap-fcAdDhVRfLn1J29tdZPVyD`, `--epochs 3`), so the artifact that actually lands will carry `epochs == 3` and the auto-activating guard would FAIL at the exact harvest moment.** Polled the run: still **RUNNING / healthy** at `date`=04:45 PDT (`modal app list` ephemeral / **1 active task**; `modal volume ls outputs/epistasis` shows only the seven prior artifacts, `gen_design.json` absent; timestamped logs show the 03:58:13 data-load print, mid-epoch-1). Left baking, did NOT relaunch (not-yet-failed). **Root cause:** iter18 wrote the guard naming the launched config "5 epochs"; iter31's re-budget (5→3, an evidence-backed exception to the no-relaunch rule) updated RUNS.md/the run but not the guard — a cross-file staleness no audit caught because the guard SKIPS today (`gen_design.json` not present), so the bad assertion is invisible until the artifact lands. **Fix (robust, not just a 3-for-5 swap):** the epoch budget is a training-budget detail that has already changed once and could change again on any future re-budget, so replaced the magic-number assertion with `assert isinstance(s["epochs"], int) and s["epochs"] >= 1` (the correctness properties `assay=="GB1_Wu"` and `positions==[265,266,267,280]` stay EXACT — those identify the right experiment; epochs does not). Verified the writer (`modal_app_v2.py::train_generative_design` lines 3261-3270) emits exactly the 11 keys the guard's `KEYS` list expects, including `library_mean_overall` — so only the epochs assertion was stale. **Proved the fix end-to-end:** simulated a realistic 3-epoch `gen_design.json` in memory — the OLD guard (`epochs==5`) returns False (would fail the tree), the NEW guard returns True and the directional claim (`generated_lib_mean_function` > random/unconditioned/overall, ≤ ceiling) holds. `pytest -q`: **122 passed / 6 skipped** (the 2 gen-design guards still skip cleanly since the artifact is absent; count unchanged). Only `tests/test_gen_design_committed.py` touched (+ this file + RUNS.md); paper untouched, no recompile needed. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the now-correct guard auto-activates → update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter36 (2026-06-26): **Verified the whole live tree green end-to-end (not asserted-from-notes) and fixed a distinct method-section numeric-precision defect — §6 hedged the e2e split as "$\sim$120 / $\sim$25" domains while the intro, §7.6, and `tab:e2e` all commit to the exact 124 trained / 25 held-out — while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:39 PDT (ephemeral, **1 active task**; `gen_design.json` NOT on the volume — `modal volume ls`/`get` show only the seven prior artifacts; ~45 min wall since the ~03:54 launch, on track to land ≈09:00 PDT inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Live-tree verification (distinct from trusting prior notes):** (a) `pytest -q` → **122 passed / 6 skipped**; (b) all **5 committed figures byte-reproduce** from committed data — regenerated fig2/fig3/fig4 via `build_epistasis_figures.py --from-committed` and fig8/fig9 via `build_model1_figure.main(out=…)` / `model2_design.figure_model2(…)` into a temp dir and `md5`-compared: all 5 PDFs **IDENTICAL** to the committed ones (DoD #5 byte-reproducibility holds on the current tree); (c) **double-blind anonymization clean** — `\usepackage{icml2025}` (no `[accepted]`), "Anonymous Authors/Institution", and grep finds no de-anonymizing URLs/github/acknowledgments/author-identifying strings in the body; (d) **ICML 8-page body limit holds** — the Conclusion (§10, the end of the body) renders on **page 8**; References + Impact Statement (both excluded from the limit) fill pp. 8-9. **Precision fix (artifact-grounded, no number invented):** `e2e_results.json` has `n_proteins=149`, `n_holdout_proteins=25` ⇒ trained on exactly 149−25=**124**, evaluated on exactly **25** — both already stated exactly in §7.6/intro/`tab:e2e`, so §6's "$\sim$120 / $\sim$25" was a loose hedge resolving to exact numbers everywhere else (a method-section internal-precision inconsistency, a distinct reviewer axis from the CI/units/layout/seed passes). Reworded §6 to "trained on the double mutants of 124 domains and evaluated on the 25 held-out domains" (149 total already stated in the same sentence, so 124+25=149 is now internally consistent). Presentation/precision-only, both counts trace to `e2e_results.json` keys → no CLAIMS/test impact. `tectonic` recompiles cleanly (**9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; verified the reworded sentence renders via `pdftotext`); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure touched — only `paper/epistasis_icml.tex`). Compiled to a throwaway `_build/` then removed it; `git status` shows only `paper/epistasis_icml.tex` (+ this file + RUNS.md). **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter35 (2026-06-26): **Closed a distinct reproducibility-reporting gap no prior iteration touched — the paper named no random seed anywhere, despite every randomized procedure in the committed code being fixed to seed 0 — while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:31 PDT (ephemeral, **1 active task**; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; timestamped logs show only the 03:58:13 PDT data-load print, mid-epoch-1 at ~93 min/epoch; a 3-epoch run + generation lands ≈09:00 PDT, inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Gap found by reading the paper as a reviewer for reproducibility:** the Implementation-details paragraph (added iter27) gave the full Model 1 architecture/optimizer/CV config but stated **no random seed**, and neither did §6/§7 — an ICML reviewer with a code release reliably asks "are these numbers seed-stable / reproducible?" **Verified the actual committed code before stating anything** (so this is faithful, not fabricated): `scripts/train_model1.py` uses `torch.manual_seed(0)` + `KFold(5, shuffle=True, random_state=0)` (GroupKFold is deterministic); `scripts/model2_design.py` uses `KFold(5, ..., random_state=0)`; `modal_app_v2.py::train_epistasis_e2e` defaults `seed=0` driving `torch.manual_seed`, `np.random.default_rng`, and `feat.sample(random_state=seed)`; all three `bootstrap_*_ci.py` use `SEED=0` over 2,000 resamples. **Fix (one sentence, grounded entirely in committed CODE → no result number, so no CLAIMS.md row, same precedent as iter27's code-grounded implementation details):** appended to the Implementation-details paragraph — "Every randomized procedure -- the cross-validation splits, the head and LoRA weight initialization, the end-to-end fine-tune's per-protein subsampling, and the 2,000-resample bootstraps used for all confidence intervals -- runs with a fixed seed (0), so the reported numbers and committed artifacts reproduce deterministically from the committed scripts." `tectonic` recompiles cleanly (**9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; verified the new sentence renders via `pdftotext`); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure touched — only `paper/epistasis_icml.tex`). Compiled to a throwaway `_build/` then removed it; no build artifacts leaked (`git status` shows only `epistasis_icml.tex` + this file + RUNS.md). **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter34 (2026-06-26): **Fixed a genuine internal-consistency defect on the paper's CENTRAL design claim — §5 (Model 2) called its candidates "every *generated* GB1 combination," directly contradicting the load-bearing non-generative framing ("Model 2 is deliberately not generative," §Related-work) that justifies NOT needing DRAKES/SVDD as matched baselines (the DoD #4 scope argument) — while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:24 PDT (ephemeral, **1 active task**; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; timestamped logs show the data-load print at 03:58:13 PDT with no epoch line yet, i.e. mid-epoch-1 at ~93 min/epoch; a 3-epoch run + generation lands ≈09:00 PDT, inside the 8 h timeout). Left baking, did NOT relaunch (not-yet-failed). **Defect found by reading §5/§Related-work for self-consistency:** §5's closed-evaluation sentence said "Because every *generated* GB1 combination is measurable in the complete *combinatorial* assay \citep{olson2014comprehensive,wu2016adaptation}…" — three precision problems: (1) "generated" contradicts the paper's own "Model 2 is deliberately not generative" (a reviewer would immediately flag a ranking policy's candidates being called "generated"); (2) Model 2 ranks GB1 **double mutants** from the **Olson complete-pairwise** assay, so citing the **four-site Wu** combinatorial assay here is a mismatch (Wu is the pending generative baseline's assay, not Model 2's); (3) "combinatorial" should be "pairwise" for the doubles task. **Fix (presentation/precision-only, no number changed → no CLAIMS.md/test impact):** reworded to "Because every *candidate* GB1 *double mutant* is measured in the complete *pairwise* assay \citep{olson2014comprehensive}, this is a closed evaluation: no selected design is unscored (Model 2 ranks measured candidates rather than generating new ones)." Dropped the Wu cite from THIS sentence only — Wu remains cited in §Related-work (l.190) + §Experiments (l.295), so it is NOT orphaned (verified: 0 bib warnings, 0 undefined). `tectonic` recompiles cleanly (**9 pp, 0 overfull hboxes, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; verified the reworded sentence renders correctly via `pdftotext`); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure touched — only `paper/epistasis_icml.tex`). Compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter33 (2026-06-26): **Eliminated all 4 overfull-hbox layout defects (tables/text spilling into the margin) — a distinct camera-ready / pixel-perfection reviewer axis no prior iteration touched — while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline): still **RUNNING / healthy** at `date`=04:17 PDT (ephemeral, **1 active task**; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"); the 3-epoch run launched ~03:54 PDT lands ≈09:00 PDT, inside the 8 h timeout. Left baking, did NOT relaunch (not-yet-failed). **Two verification passes first (both clean, no drift):** (1) all 19 `\cite` keys resolve in `epistasis.bib` (0 missing); (2) every headline number in the body matches its committed artifact JSON exactly (decomposition −0.91/0.72/0.42/28%, headroom 0.25/0.23/60%, model1 GB1 0.021/0.36/0.12 + GFP 0.005/0.14/0.08, e2e 0.33/0.33/0.80, model2 −3.62/−2.53/−3.84/−2.00/+1.09/−0.22/0.28). **Defect found by compiling and reading the `.log`:** the body had **4 overfull `\hbox`es** — `tab:model1` 71.5pt over (last column ran into the gutter), §7.2 a 46.3pt-over unbreakable `\texttt{...zeroshot\_spearman}` path, `tab:model2` 61.4pt over (long "Best-of-$N$ rerank (zero-shot DPLM PLL)" label + wide header), and `tab:decomp` 4.2pt over (the 71/61/46pt ones genuinely run text into the margin — exactly what an area chair flags for camera-ready). **Fix (presentation-only, no number changed → no CLAIMS.md/test impact):** `tab:model1` now uses a stacked `\multicolumn{2}{c}{Model 1}` + `\cmidrule` header (cols just "Doubles"/"Position") with `\tabcolsep` 5pt; `tab:model2` shortened the header to "Top-10\%" (caption already defines it as binding) and the label to "Best-of-$N$ (zero-shot DPLM PLL)" with `\tabcolsep` 5pt; the §7.2 path shortened to the wildcard `model1\_*\_summary.json:zeroshot\_spearman` (same style already used in `tab:model1`'s caption); `tab:decomp` got `\tabcolsep` 4pt. `\setlength` is scoped to each `table` env so no leak to the un-touched zeroshot/e2e tables. `tectonic` recompiles cleanly (**9 pp**, **0 overfull hboxes** (was 4), 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder; verified the edited tables still render correct headers + numbers via `pdftotext`); `pytest -q`: **122 passed / 6 skipped** (no code/data/figure touched — only `paper/epistasis_icml.tex`). Compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter32 (2026-06-26): **Filled the one remaining CENTRAL scholarship gap — the coevolution / direct-coupling related work (a reviewer-facing hole iter23 left open by covering only the generative/design side) — while the 3-epoch generative-design run keeps baking.** Polled `ap-fcAdDhVRfLn1J29tdZPVyD` (the DoD #4 discrete-diffusion guidance baseline, iter31's 3-epoch replacement): still **RUNNING / healthy** (ephemeral, 1 active task; `gen_design.json` NOT on the volume; `modal app logs --timestamps` shows the data-load print at **03:58:13 PDT** with no epoch line yet and no traceback, so it is early in epoch 1; `date`=04:09 PDT). At ~93 min/epoch a 3-epoch run + generation lands ≈ 09:00 PDT, well inside the 8 h timeout; left baking, did NOT relaunch (not-yet-failed). **Reviewer gap found by reading the related-work section against the paper's CORE thesis:** the paper claims specific pairwise epistasis is *predictable from sequence representations*, yet Related Work cited the global-epistasis, zero-shot-PLM, assay-labeled, and generative lines but **zero coevolution / direct-coupling (DCA) work** — the single most-expected related literature for any "predict pairwise couplings from sequence" paper (a 15-year line a subfield reviewer would immediately miss). iter23's citation pass only filled the *generative/design* side, not this. **Fix (dry, web-verified, no numbers → no CLAIMS.md change):** added 3 real bib entries (Morcos et al. 2011 PNAS 108:E1293, DCA → native contacts; Hopf et al. 2017 Nat. Biotech. 35:128, EVmutation → epistasis-aware unsupervised mutation effects; Poelwijk et al. 2019 Nat. Commun. 10:4213, measured sparse combinatorial epistasis) and a new `\paragraph{Coevolution and direct-coupling models.}` that positions them honestly as **related context, not a matched baseline**: coevolutionary couplings are estimated from cross-species alignment covariation and capture an average, evolutionarily/structurally constrained interaction, whereas our target is the **assay-specific functional epistasis residual of a single protein's measured combinations after the additive + global layers are removed** — which family covariation does not directly isolate. `tectonic` recompiles cleanly (**9 pp total — body (Intro→Conclusion) ends at the top of page 8, within the ICML 8-page content limit; References + Impact Statement on pp. 8-9 are excluded; 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; all 3 new cites resolve in body + bibliography); `pytest -q`: **122 passed / 6 skipped** (no code touched). Only `paper/epistasis_icml.tex` + `paper/epistasis.bib` (+ this file + RUNS.md) touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + the pre-staged guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter31 (2026-06-26): **Acted on the iter30 timeout watch: stopped the provably-doomed 5-epoch generative-design run and launched a viable 3-epoch replacement (the sole remaining DoD #4 gate).** Confirmed the diagnosis with timestamped container logs (`modal app logs --timestamps`): `[gen] epoch 1/5` completed at **03:41:45 PDT** = ~103 min after the 01:58 launch, i.e. ~90-100 min/epoch (the print is end-of-epoch, so this is real progress, not buffered lag). With 4 epochs left + generation and `gen_design.json` written only after ALL 5 epochs + generation (no checkpoint), the run cannot finish before the **09:58 PDT (8 h) timeout** → it would be killed having written nothing. Rather than burn ~6 more H100-hours waiting for the 09:58 watch trigger to confirm a *certain* failure, `modal app stop`ped `ap-BlbHQjQn930EN3cSt5zDpZ` and launched **`ap-fcAdDhVRfLn1J29tdZPVyD` with `--epochs 3`** (same function/assay/config otherwise) — fits the 8 h timeout with a ~3 h margin (~304 min: 10 min setup + 3×~93 min + ~15 min generation), and 3 epochs (~11.9k LoRA grad steps over 127k reward-weighted seqs) is ample for the directional generative-baseline claim. Deliberate, evidence-backed exception to "do not relaunch a not-yet-failed run": the run was **misconfigured** (epochs > timeout budget), so this is reconfiguration, not duplication. Verified healthy: image cache-hit build, function body executing (`[gen] GB1_Wu: 4 editable positions [265,266,267,280], 149360 measured combos`), DPLM downloading, **1 sustained active task surviving the local client kill**; old run confirmed `stopped` (0 tasks). This is a launch-only minimal iteration (no code/paper/test changes — only RUNS.md/PROGRESS.md), so the tree stays green by construction (122 passed / 6 skipped as of iter30). HARVEST when `gen_design.json` lands (~09:00 PDT expected): pull + commit it, fold a grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row, the pre-staged `tests/test_gen_design_committed.py` guard auto-activates, update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences, final whole-DoD re-audit → flip PROGRESS to COMPLETE. **This is the last substantive gate to ICML_READY.**

- iter30 (2026-06-26): **Added the ICML-required Impact Statement section (a hard submission-readiness requirement no prior iteration caught), and made a genuinely new diagnosis of the generative-design run from its actual epoch logs.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — volume `outputs/epistasis/` ls shows only the seven prior artifacts; ~110 min real wall since the 01:58 PDT launch, `date` = 03:47 PDT); left baking, did NOT relaunch per the objective (not-yet-failed). **NEW FINDING (corrects the prior "near completion since iter24" framing):** unlike every prior poll, this iteration actually tailed the container logs — they show only `[gen] epoch 1/5 weighted-NLL 1.4935`, i.e. the run is at **epoch 1 of 5** after ~110 min, NOT near completion. The function decorator (`modal_app_v2.py:3116`) has `timeout=3600*8` (8 h). If epochs run ~100 min each, 5 epochs ≈ 8.3 h would EXCEED the 8 h timeout and the run would be killed before writing `gen_design.json` — so the prior notes' "won't die from timeout" is at risk. **Watch item:** if `gen_design.json` has not landed by ≈09:58 PDT (8 h after launch), the run has hit the timeout and a future iteration should relaunch with fewer epochs (e.g. `--epochs 2`) on H100. (Caveat: the single log line may be buffered-stdout-lagged; epoch 1 could have finished earlier, leaving ~80 min/epoch → ~6.7 h total < 8 h, which would land — so it is at-risk, not certain to fail.) **Dry unit (the actual submission-readiness fix):** ICML 2025 requires every submission to carry an Impact Statement (placed after Conclusion, before References, excluded from the 8-page body limit); the paper had none. Added a tailored `\section*{Impact Statement}` engaging honestly with protein-design dual-use (beneficial engineering applications; in-silico-only / benign public assays GB1·GFP·stability / no wet-lab so immediate misuse risk is low; correlations far from substituting for measurement; biosecurity-screening encouragement) — qualitative, no number, nothing to add to CLAIMS.md. `tectonic` recompiles cleanly (**9 pp** total — the Impact Statement + references push past 8, both excluded from the page-limited body, which is unchanged in length since the statement is appended after the Conclusion; **0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**); `pytest -q`: **122 passed / 6 skipped** (no code touched). Only `paper/epistasis_icml.tex` (+ this file + RUNS.md) touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking — watch the timeout) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter29 (2026-06-26): **Added the missing measurement units to the central Model 2 design result (a distinct reviewer axis no prior audit touched), while the generative-design run keeps baking.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; volume `outputs/epistasis/` ls shows only the seven prior artifacts, no partial gen output; ~102 min real wall since the 01:58 PDT launch, `date` = 03:40 PDT); left baking, did NOT relaunch. Reviewer gap found by inspecting the committed data scale: the Model 2 table/text reported "mean measured binding" lifts (`−3.62`, `+1.09`, etc.) with **no stated units**, while the decomposition correctly states kcal/mol. Verified from the committed `model2_oof_GB1.csv` that the `measured` column is floored at exactly `−4.60517 = ln(0.01)` (max `2.626`), i.e. the GB1 values are the dimensionless **log-scale ProteinGym DMS score** (binding-enrichment), not a linear binding quantity — so a reviewer could not interpret the lift scale. **Fix (dry, no GPU, no number changed — only a units clarification grounded in the committed data scale):** added one sentence to §7.5 ("Measured binding is the ProteinGym DMS score for the Olson GB1 assay — a dimensionless log-scale binding-enrichment value — so the lifts below are on that log scale") and a parenthetical to the `tab:model2` caption ("log-scale ProteinGym DMS score, dimensionless"). Also confirmed the §5/§7.5 "additive prediction" pool/bin wording is NOT a bug: the code thresholds/bins on the monotonic global prediction (`ddG_global = φ(ddG_additive)`), which gives the identical median split and identical deciles, so "additive" is correct under monotonic equivalence — left unchanged. No new result number introduced (the binding values were already in CLAIMS.md → `model2_GB1_summary.json`), so CLAIMS.md is unaffected; the units statement is a descriptive assay fact (like iter27's code-grounded implementation details). `tectonic` recompiles cleanly (**8 pp, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**; the only warning is a pre-existing cosmetic overfull hbox in the fig9 caption); `pytest -q`: **122 passed / 6 skipped** (no code touched). Only `paper/epistasis_icml.tex` (+ this file + RUNS.md) touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter28 (2026-06-26): **Fixed a genuine honesty + traceability gap in the headline e2e comparison (DoD #8 / hard-rule): the paper's "$0.25$ zero-shot reference" was being traced to a HARDCODED literal, not a measured baseline, and was framed as a per-test-protein control it never was.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; ~94 min real wall since the 01:58 PDT launch, confirmed by `date` = 03:32 PDT; estimated training runtime ~70–90 min for 5 epochs over ~127k GB1_Wu seqs at batch 32 on H100, so it is plausibly in the generation/finalize stage and near completion); left baking, did NOT relaunch. **Reviewer gap found by tracing the e2e 0.25 to its source:** `e2e_results.json:zero_shot_reference` is a hardcoded constant `0.25` in `modal_app_v2.py::train_epistasis_e2e` (line 3107), NOT a zero-shot baseline measured on the 25 held-out proteins; the real measured zero-shot 0.25 is `headroom_summary.json:median_per_protein_spearman` (0.250987) on a DIFFERENT 30-domain subset. The paper (§7.6 + abstract + contributions + `tab:e2e` caption) presented "0.33 above the 0.25 zero-shot reference on the same residual target on proteins the head never saw," implying the 0.25 was a per-held-out-protein control. **Fix (dry, no GPU, no number changed — only provenance + framing):** (1) §7.6 now states the 0.25 is the corpus-level zero-shot DPLM PLL ceiling on the Megascale stability corpus (same specific-epistasis target/metric/regime, `tab:zeroshot`), explicitly NOT re-measured on the exact 25 held-out domains, and notes a per-held-out-protein zero-shot control is future work; the 0.33/mean/80% trace to `e2e_results.json`, the 0.25 to `headroom_summary.json`; (2) `tab:e2e` caption corrected to source the 0.25 from `headroom_summary.json`; (3) abstract + contributions reworded ("corpus-level zero-shot DPLM PLL reference on the same stability task"); (4) CLAIMS.md row 122 re-traced the 0.25 to `headroom_summary.json:median_per_protein_spearman` with a note that `e2e_results.json:zero_shot_reference` is a hardcoded literal that merely coincides. `tectonic` recompiles cleanly (**8 pp, 0 undefined refs/cites, 0 `??`**); `pytest -q`: **122 passed / 6 skipped** (no code touched; `tests/test_e2e_artifact.py` still guards the artifact, including the hardcoded `zero_shot_reference` key, which is unchanged). Only `paper/epistasis_icml.tex` + `CLAIMS.md` (+ this file + RUNS.md) touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter27 (2026-06-26): **Added a reviewer-required "Implementation details" paragraph + defined the previously-undefined `LL` operator in eq:zs, all grounded in committed code, while the generative-design run keeps baking.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; ~87 min real wall since the 01:58 PDT launch, confirmed by `date` = 03:25 PDT; app logs empty due to Modal's buffered-stdout lag but healthy — 1 sustained active task, no traceback); left baking, did NOT relaunch. Reviewer gap found by reading the compiled paper: (1) §sec:model1 / Experiments described Model 1 conceptually but gave **no concrete architecture or training config** — a main-conference reviewer reliably docks reproducibility for this; (2) eq:zs used the operator `LL(·)` which was never defined. Fixed both from the COMMITTED source (`scripts/train_model1.py::CouplingHead`/`train_fold` for the head + AdamW/Huber/200-epoch/batch-1024/5-fold config, and `modal_app_v2.py` for the summed-residue-logprob PLL) — no fabrication, all values read from committed code: added a faithful `\paragraph{Implementation details.}` (dproj 128, bilinear $p_i\odot p_j$, 16-d AA embeddings ×4, z-scored PLL feature + learnable PLL gain, LayerNorm+MLP 256→128, dropout 0.3, AdamW lr $2\times10^{-3}$/wd $10^{-4}$, 200 epochs, batch 1024, smooth-$L_1$ on per-fold-standardized targets, KFold-5 doubles / GroupKFold-5-by-position, z-scored features, no position indices) pointing at `scripts/train_model1.py`, and defined `LL(x)` as DPLM's summed-residue pseudo-log-likelihood. `tectonic` recompiles cleanly (**8 pp, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**); `pytest -q`: **122 passed / 6 skipped** (no code touched — only `epistasis_icml.tex`). Compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter26 (2026-06-26): **Added bootstrap 95% CIs to the Model 1 held-out-POSITION split (the one remaining result number with no uncertainty quantification), confirming the "still clears the floor" claim is significant, while the generative-design run keeps baking.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; ~80 min real wall since the 01:58 PDT launch, confirmed by `date` = 03:19 PDT; app logs empty due to Modal's buffered-stdout lag but healthy: 1 sustained active task, no traceback); left baking, did NOT relaunch. Reviewer gap found by reading §7.3: iter21 gave the held-out-DOUBLES Spearman a CI, but the harder held-out-POSITION numbers (GB1 $0.12$, GFP $0.08$) carried the claim "still clears the floor" with NO CI — and on GFP, where the doubles zero-shot CI already includes zero, a reviewer would reasonably ask whether $0.08$ on the position split is even distinguishable from chance. Fixed using only committed data: extended `scripts/bootstrap_model1_ci.py` to also resample the committed `oof_position` column on the SAME resamples (the position split's null is chance, Spearman 0, so a CI excluding 0 is the significance test). Real result — BOTH position CIs exclude zero: GB1 $0.12$ (95\% CI $[0.10, 0.14]$), GFP $0.08$ ($[0.06, 0.10]$), so even this cross-position transfer is significantly above chance, confirming (not weakening) the existing claim. Regenerated `outputs/epistasis/model1_ci.json` (verified byte-identical on a second run, seed 0), folded one CI clause into §7.3, added 2 CLAIMS.md rows + the no-drift/significance guard into `tests/test_model1_ci.py` (now also asserts the position point estimates match the committed summaries and the position CIs exclude 0). `tectonic` recompiles cleanly (**8 pp, 0 undefined refs/cites, 0 `??`**); `pytest -q`: **122 passed / 6 skipped** (unchanged count — extended an existing test, no new file). Only `bootstrap_model1_ci.py` + `model1_ci.json` + `epistasis_icml.tex` + `CLAIMS.md` + `test_model1_ci.py` + this file + RUNS.md touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter25 (2026-06-26): **Added bootstrap 95% CIs + significance tests to the three-layer DECOMPOSITION result (the paper's "strongest, fully-committed result", previously bare point estimates), completing the "every committed result block has a CI" rigor arc, while the generative-design run keeps baking.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; ~75 min real wall since the 01:58 PDT launch, confirmed by `date` = 03:13 PDT; app logs still empty due to Modal's buffered-stdout lag but healthy — 1 sustained active task, no traceback, vs the A10G-OOM failure that dropped to 0 tasks in ~2 min); left baking, did NOT relaunch. Reviewer gap found by reading the compiled paper: iter21 gave Model 1's *prediction* numbers CIs and iter24 the Model 2 *design* result CIs, but §7.1 / Table 1 — the decomposition the limitations section calls "our strongest, fully-committed result" — still had bare point estimates ($R^2_{\text{add}}=-0.91$, $R^2_{\text{glob}}=0.72$, spec std $0.42$, share $28\%$), so a reviewer could ask whether the typical-protein medians are robust to protein sampling. Fixed using only committed data: new `scripts/bootstrap_decomposition_ci.py` does a 2,000-resample percentile bootstrap over the 149 per-protein rows of the committed atlas (seed 0), recomputing the EXACT paper statistic (`build_decomposition_artifact.summarize`, reused — no statistic duplication) on each resample, writing committed `outputs/epistasis/decomposition_ci.json`. Real result, all three decomposition claims exclude their null: median additive $R^2$ $-0.91$ (95\% CI $[-1.39, -0.58]$, entirely $<0$ — additive worse than the mean for the typical protein); median $+$global $R^2$ $0.72$ ($[0.67, 0.74]$, entirely $>0$ — the monotonic link genuinely recovers the gap); median specific std $0.42$ kcal/mol ($[0.39, 0.45]$) at a $28\%$ ($[0.26, 0.33]$) variance share. Folded one grounded sentence into §7.1 + 4 CLAIMS.md rows; guarded by `tests/test_decomposition_ci.py` (no-drift of point estimates + the three significance CIs exclude 0; small-B re-bootstrap, fast). `tectonic` recompiles cleanly (**8 pp** total — body + references, body within the ICML 8-page content limit since references are excluded; **0 undefined refs/cites, 0 `??`**, new CI sentence + `decomposition_ci.json` source render); `pytest -q`: **122 passed / 6 skipped** (was 120/6; +2 new CI guards). Only the 3 new files + `epistasis_icml.tex` + CLAIMS.md + this file + RUNS.md touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter24 (2026-06-26): **Added bootstrap 95% CIs + significance tests to the Model 2 DESIGN result (the paper's central design claim, previously zero uncertainty quantification) from committed data, while the generative-design run bakes.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"; ~66 min real wall since the 01:58 PDT launch, confirmed by `date` = 03:04 PDT; app logs still empty due to Modal's buffered-stdout lag but healthy — 1 sustained active task, no traceback); left baking, did NOT relaunch. Reviewer gap found by reading the compiled paper: iter21 gave Model 1's *prediction* numbers CIs, but the Model 2 *design* result — the more central claim ("design gain-of-function combinations additive models cannot find") — was still bare point estimates (lift $+1.09$, matched-additive control $0.28$), so a reviewer could ask whether the $0.28$ control is distinguishable from 0. Fixed using only committed data: new `scripts/bootstrap_model2_ci.py` does a 2,000-resample percentile bootstrap over the 12,000 held-out GB1 doubles of the committed `model2_oof_GB1.csv` (seed 0), recomputing the EXACT paper statistic (`summarize_model2`) on each resample so the matched-additive median-over-deciles and the top-10%-of-pool lifts are bootstrapped self-consistently, writing committed `outputs/epistasis/model2_ci.json`. Real result: matched-additive control Spearman $0.28$ (95% CI $[0.24, 0.31]$, excludes 0 — the recovered gain is genuine epistasis, not relearned additivity); Model 1 design lift $+1.09$ ($[0.99, 1.20]$); and on the same resample Model 1's lift exceeds the zero-shot DPLM PLL rerank lift by $+1.31$ ($[1.17, 1.44]$) — ranking by predicted epistasis *significantly* beats the PLM-as-reward best-of-$N$ baseline. Folded one grounded sentence into §7.4 + 4 CLAIMS.md rows; guarded by `tests/test_model2_ci.py` (no-drift of point estimates + the three significance CIs exclude 0; small-B re-bootstrap, fast). Also refactored `scripts/model2_design.py` to lazy-import the heavy `train_model1`/torch deps inside `build_oof_table`, so `summarize_model2` (used by two tests + the CI bootstrap) imports without pulling torch (2.4 s → 1.3 s). `tectonic` recompiles cleanly (**8 pp** — body ends mid-page 7, references span 7–8; well within the ICML 8-page content limit — **0 undefined refs/cites, 0 `??`**); `pytest -q`: **120 passed / 6 skipped** (was 118/6; +2 new CI guards). Only the 3 new files + `epistasis_icml.tex` + `model2_design.py` + CLAIMS.md + this file + RUNS.md touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter23 (2026-06-26): **Grounded the paper's discrete-diffusion / reward-guided generative-design references in the real literature (reviewer-facing scholarship gap) while the generative run bakes.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory"). Confirmed via `date` that only **~1 h of real wall-clock has elapsed since the 01:58 PDT launch** (now 02:59 PDT) — the 8-iteration span (iter15→iter23) is <1 h of real time because each iteration is a fast dry-work turn; the 5-epoch reward-weighted DPLM-650M fine-tune over ~127k GB1_Wu seqs is genuinely multi-hour, so this is healthy, not stuck. Left baking, did NOT relaunch. Reviewer gap found by reading the compiled paper + bib: the paper twice gestures at "discrete-diffusion guidance and reward-guided refinement" comparators (§7 Baselines, §9 Limitations) but the bib cited **zero** such methods — a scholarship hole a reviewer in this exact subfield would flag, and a hole the pending `gen_design.json` harvest will need filled regardless. Web-verified the four canonical methods and added real, accurate bib entries: NOS (Gruver et al., NeurIPS 2023, 2305.20009), discrete-state guidance (Nisonoff et al., ICLR 2025, 2406.01572), DRAKES reward fine-tuning (Wang et al., ICLR 2025, 2410.13643), SVDD value-based decoding (Li et al., 2024, 2408.08252). Added a new Related-Work paragraph ("Generative and guided protein design") citing all four and stating honestly that Model 2 is a non-generative ranking policy over an already-measured pool (so guided generation is related context, not a matched baseline; the best-of-$N$ reranking reduction is what applies to a closed pool), and added the four `\citep`s to the two existing §7/§9 gesture points. No numbers added (pure scholarship), so CLAIMS.md is unaffected. `tectonic` recompiles cleanly (**8 pp** — +1 page from the new paragraph + 5 bib entries; body still within the ICML 8-page limit since the extra page is references, **0 undefined refs/cites, 0 `??`**, all four new cites resolve in body + bibliography). `pytest -q`: **118 passed / 6 skipped** (no code touched). Only `paper/epistasis_icml.tex` + `paper/epistasis.bib` touched; compiled to `/tmp`, no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection (now able to cite DRAKES/SVDD/NOS as the comparator family) + CLAIMS row + guard auto-activates + update the §7/§9 "we do not benchmark discrete-diffusion guidance" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter21 (2026-06-26): **Added bootstrap 95% CIs + a significance test to the headline Model 1 prediction numbers (reviewer-facing rigor, pure committed data) while the generative-design run bakes.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory") — 7th iteration of the multi-hour H100 fine-tune (~50 min wall since the 01:58 PDT launch; logs still show only the data-load print + HF download warning due to buffered-stdout lag, no traceback); left baking, did NOT relaunch. Reviewer gap found by reading the compiled paper: every headline number (Model 1 0.36/0.14, zero-shot 0.021/0.005, e2e 0.33) was a bare POINT ESTIMATE with no uncertainty — a main-conference reviewer would ask whether the gain over the near-zero zero-shot floor is significant. Fixed for the Model 1 prediction claim using only committed data: new `scripts/bootstrap_model1_ci.py` does a 2,000-resample percentile bootstrap over the held-out doubles of the committed `model1_oof_{GB1_Olson,GFP}.csv` (seed 0, paired Model 1 − zero-shot difference per resample), writing the committed `outputs/epistasis/model1_ci.json`. Real result: GB1 Model 1 Spearman 0.36 (95% CI [0.34, 0.38]), GFP 0.14 ([0.12, 0.16]); the gain over zero-shot is significant on both (delta 95% CI [0.31, 0.36] and [0.11, 0.16], excluding 0); and the **GFP zero-shot CI [−0.01, 0.02] includes zero** — the zero-shot PLL is statistically indistinguishable from chance on GFP function, quantitatively sharpening the "invisible to zero-shot" claim. Folded one grounded sentence into §7.3 + 6 CLAIMS.md rows; guarded by `tests/test_model1_ci.py` (no-drift of point estimates + delta CI excludes 0, fast — small-B re-bootstrap, 2.2 s). `tectonic` recompiles cleanly (**7 pp, 0 undefined refs/cites, 0 `??`**); `pytest -q`: **118 passed / 6 skipped** (was 116/6; +2 new CI guards). Only the 3 new files + `epistasis_icml.tex` + CLAIMS.md + this file + RUNS.md touched; no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection + CLAIMS row + guard auto-activates + update the §5/§9 "we do not make a generative claim" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter20 (2026-06-26): **Made the paper figures byte-reproducible (DoD #5 hardening) while the generative-design run bakes.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory") — 6th iteration of the multi-hour H100 fine-tune (~42 min wall since the 01:58 PDT launch; iterations are fast turns); left baking, did NOT relaunch (objective forbids re-launching a not-yet-failed run). First **verified the live tree** rather than trusting asserted status: `pytest -q` → 116 passed / 6 skipped; regenerated all 5 paper figures from committed data (`build_epistasis_figures.py --from-committed`, `build_model1_figure.py`, `model2_design.py::figure_model2`) with passing self-checks (GB1 0.360/0.021, GFP 0.138/0.005; median $R^2$ −0.912→+0.721, spec std 0.422) — DoD #5/#7 hold. **Found a real reproducibility defect:** rerunning the figure scripts dirtied the tree because matplotlib embeds a `CreationDate` timestamp in PDF metadata (the regenerated `fig2` carried `/CreationDate (D:2026...)`), so the bytes differ on every regen even though the content is identical — defeating the "regenerate-and-diff to verify" point of DoD #5's reproducibility claim. **Fix:** added `metadata={"CreationDate": None}` to the PDF `savefig` in the three paper-figure scripts (`build_epistasis_figures.py::_save`, `build_model1_figure.py`, `model2_design.py::figure_model2`), then **verified determinism empirically** — regenerated all 5 figures twice and confirmed md5-identical across both passes (BYTE-IDENTICAL). Re-emitted the 5 committed figure PDFs timestamp-free. `tectonic` recompiles cleanly with the new figures (**7 pp, 0 undefined refs/cites, 0 `??`**); `pytest -q` still 116 passed / 6 skipped (the edited `model2_design.py` is imported by `tests/test_model2_committed.py`, which still passes). Only the 3 figure scripts + 5 regenerated PDFs + this file touched; PNGs unchanged (already deterministic), no build artifacts leaked. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection + CLAIMS row + guard auto-activates + update the §5/§9 "we do not make a generative claim" sentences (the discrete-diffusion guidance run becomes a benchmarked comparator, still no generative claim of our own) → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter19 (2026-06-26): **Honesty + scope audit of the compiled paper while the generative-design run bakes — fixed a real cross-protein overclaim (DoD #9) and verified DoD #6.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume — `modal volume get` → "No such file or directory") — 5th iteration of the multi-hour H100 fine-tune; left baking, did NOT relaunch (objective forbids re-launching a not-yet-failed run). Read the full paper as a reviewer would and found one genuine honesty gap: the headline cross-protein generalization number (median Spearman **0.33**, the "skill transfers across proteins" claim) is measured on the **Megascale STABILITY corpus** (confirmed `e2e_results.json`: `n_proteins 149`, `n_doubles 120000` = Megascale), yet the abstract / §6 / §7.6 never said so — a reader would conflate it with the paper's FUNCTION thesis. Fixed across four spots (abstract, contributions bullet, §6 method, §7.6 results): each now states the cross-protein test runs on the stability corpus because it is the only corpus with enough proteins (149 domains) for a held-out-PROTEIN split, and the **limitations section** now honestly states that cross-protein transfer of the FUNCTIONAL skill specifically would need more combinatorial functional assays than exist and remains future work. Also verified **DoD #6**: grep for `calibrat|temperature scal|ECE|conformal` over the `.tex` returns nothing — no calibration claim is retained (the only `isotonic` is the global-epistasis link, reported as a held-out residual), so DoD #6 → ☑ (vacuously satisfied). Corrected the STALE DoD #1 note ("PENDING: fold in numbers" — those were all folded in iter13/14; only the generative subsection remains). `tectonic` recompiles cleanly (**7 pp, 0 undefined refs/cites, 0 `??`, 0 TODO/fbox/placeholder**). `pytest -q`: **116 passed / 6 skipped** (no code touched). Only `paper/epistasis_icml.tex` + this file touched. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4, still baking) → grounded generative-design subsection + CLAIMS row + guard auto-activates + update the §5/§9 "we do not make a generative claim" sentences → final whole-DoD re-audit → flip PROGRESS to COMPLETE.**
- iter18 (2026-06-26): **Pre-staged the DoD #4 generative-design guard test while the run keeps baking** (dry, verifiable). Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT on the volume; `modal volume get` → "No such file or directory") — 4th iteration of the multi-hour 5-epoch reward-weighted DPLM-650M fine-tune (~127k GB1_Wu train seqs, batch 32, nested per-position Python loss loop, H100, 8h timeout so it won't die from timeout). Left baking. Did the documented harvest-prerequisite dry unit: wrote `tests/test_gen_design_committed.py` — a skip-until-present guard (the iter8/iter12 pattern) that activates the moment `gen_design.json` is committed and skips cleanly until then. Read the writer (`modal_app_v2.py::train_generative_design`, summary keys + `combo_score=DMS_score`, `tr_w=rank(pct)` so higher function is better) to ground the assertions: (1) structural validity that holds regardless of outcome — all 11 summary keys present, `assay=="GB1_Wu"`, `positions==[265,266,267,280]`, `epochs==5`, `coverage∈[0,1]`, `n_novel_generated>=0`; (2) the paper's generative claim — `generated_lib_mean_function` beats `random_lib_mean`, `unconditioned_DPLM_mean`, and `library_mean_overall`, and is `<= top_library_ceiling`. No hardcoded result numbers (the run hasn't produced any), so nothing fabricated. `pytest -q`: **116 passed / 6 skipped** (the 2 new guard tests skip cleanly; tree green). Only the new test + PROGRESS.md + RUNS.md touched; paper untouched (recompile not needed). **HARVEST next iteration (now a pure commit+fold step):** `modal volume get phaseagent-data outputs/epistasis/gen_design.json .`, commit it (the guard auto-activates — re-run `pytest -q` to confirm the real artifact passes), fold a grounded generative-design subsection + CLAIMS.md row into the paper, recompile — completing DoD #4 (dissolving BLOCKERS.md B1), then the final whole-DoD re-audit + flip PROGRESS to COMPLETE → `ICML_READY`.
- iter17 (2026-06-26): **DoD #8 final-audit pass on the compiled paper, while the generative-design run bakes.** Polled `ap-BlbHQjQn930EN3cSt5zDpZ` (the DoD #4 discrete-diffusion guidance baseline): still RUNNING (ephemeral, 1 active task; `gen_design.json` NOT yet on the volume) — the 5-epoch reward-weighted DPLM-650M fine-tune over ~127k GB1_Wu train sequences on H100 is multi-hour; left baking, harvest next iteration. Did the highest-value dry unit: **extracted every quantitative number in the compiled body and cross-checked it against CLAIMS.md + the actual committed artifacts** (re-read all six summary JSONs to confirm values). Findings + fixes (all dry, verifiable): (1) the §7.1 decomposition text claimed the specific-epistasis median was "roughly four times the $\sim 0.1$ kcal/mol measurement noise of the assay" — an unbacked number (no committed artifact) and inconsistent with fig3's own illustrative band (`axvspan(0.1, 0.3)`); **removed it**, re-grounding the "not noise" claim on the committed variance-share rows (median specific share 28%, $\geq 10\%$ in 98% of proteins) and softening the fig3 caption ("far above" → "sits above the illustrative measurement-scatter band"); (2) added the missing CLAIMS.md rows for body numbers that WERE artifact-backed but unrowed — the §7.3 zero-shot-gain deltas (GB1 +0.34 / GFP +0.13 = `model1_*_summary.json:delta_doubles`) and the e2e LoRA rank 64 / 10 epochs (`e2e_results.json:lora_r,epochs`); (3) normalized the fig9 `\includegraphics` path (dropped a redundant `figures_epistasis/` prefix that only resolved via the cwd fallback) for consistency with the other 4 figures. Documented in CLAIMS.md that the fig3 grey band is illustrative-only, not a committed result. `tectonic --keep-logs` recompiles cleanly (exit 0, 7 pp, **0 final-pass undefined refs/cites**, 0 `??` in the rendered PDF, 5 `\includegraphics`; remaining log warnings are benign Times-font substitutions). `pytest -q`: **116 passed / 4 skipped** (no code touched). Tree green; only `paper/epistasis_icml.tex` + `CLAIMS.md` + this file + RUNS.md modified. **Remaining for ICML_READY: harvest `gen_design.json` (DoD #4 discrete-diffusion guidance baseline, still baking) → fold a grounded generative-design subsection + CLAIMS row + guard test (dissolves BLOCKERS.md B1), then the final whole-DoD re-audit + flip PROGRESS to COMPLETE.**
- iter16 (2026-06-26): **Resolved DoD #5 (figures): every committed figure is now cited AND regenerates from committed data; added a real Model 1 "predict" figure; deleted the orphans.** The generative-design run (`ap-BlbHQjQn930EN3cSt5zDpZ`) is still RUNNING (1 active task, `gen_design.json` not yet on the volume) — left baking, did dry work. Audit found 7 committed figures the paper did NOT cite (fig1, fig4, fig5, fig6, fig7, fig7a, fig8) — exactly the BLOCKERS.md orphan-figure final-audit item, and a latent DoD #5 gap: even the CITED fig2/fig3 only regenerated via `build_epistasis_figures.py` reading `/tmp/epi_decomposed.parquet` (NOT committed). Fixes: (1) **rewrote `scripts/build_model1_figure.py`** to read the committed `model1_oof_{GB1_Olson,GFP}.csv` (was reading `/tmp/feat_*.npz` + retraining with torch) → produces fig8 with a self-check `assert` that the drawn Spearmans equal the committed `model1_*_summary.json` (GB1 0.36/0.021, GFP 0.14/0.005); (2) **added a `--from-committed` path to `build_epistasis_figures.py`** that regenerates fig2/fig3 from the committed `three_layer_atlas.parquet` and fig4 from the committed `headroom_dplm.parquet`, no `/tmp`; verified the numbers match `decomposition_summary.json` (additive −0.912 → global 0.721, spec std 0.422, n=149); (3) **cited fig4 in §7.2** (zero-shot headroom, `\cref{fig:headroom}`) and **fig8 in §7.3** (Model 1 predict, `\cref{fig:model1}`) — both regenerate from committed data and are on-message, no fabricated numbers; (4) **deleted the 5 genuine orphans** (fig1, fig5, fig6, fig7, fig7a — uncited, regenerated only from non-committed `/tmp` raw assay CSVs; confirmed unreferenced in any `.tex`/`.md`). End state: committed figures = fig2, fig3, fig4, fig8, fig9, ALL cited, ALL committed-data-regenerable. `tectonic` recompiles cleanly (exit 0, 7 pp, 0 undefined refs/cites, 0 `??`, 5 `\includegraphics`). `pytest -q`: **116 passed / 4 skipped**. CLAIMS.md figures table updated (5 rows + the no-orphan statement); BLOCKERS.md orphan-figure checklist item resolved. **Remaining for ICML_READY: harvest `gen_design.json` (the DoD #4 discrete-diffusion guidance baseline, still running), then the final whole-DoD re-audit + B1 resolution.**
- iter15 (2026-06-26): **Launched the actual DoD #4 discrete-diffusion guidance baseline (resolving B1 by computation, not by a scope argument).** The only substantive gate left to `ICML_READY` was DoD #4's "one strong discrete-diffusion guidance baseline", which iter14 flagged as a human scope call (`BLOCKERS.md` B1: argue it scoped-out because the paper makes a ranking claim). The cleaner, more honest path is to COMPUTE the baseline the DoD literally names — and the objective explicitly points at `train_generative_design` as "Generative Model 2". Read it: it is a function-weighted (reward-guided) LoRA fine-tune of DPLM-650M (a discrete-diffusion PLM) that GENERATES high-function multi-mutants via iterative masked-diffusion sampling over the editable positions, evaluated on a COMPLETE combinatorial assay so every generated combo is measurable — i.e. exactly a strong discrete-diffusion guided-generation baseline. Verified the GB1_Wu assay CSV is on the volume (149,360 measured combos, 4 sites) and that GB1_Wu (not GB1_Olson) is the correct assay (the generative procedure edits all editable positions and needs full measurability; Olson's pairwise-only design → ~0 coverage). **Launched `modal run --detach modal_app_v2.py::train_generative_design`**; the `gpu_lora_image` build was a cache hit, and I stayed live until the function body provably executed (`[gen] GB1_Wu: 4 editable positions [265,266,267,280], 149360 measured combos`, DPLM weights loading, 1 active task). The first launch on A10G (`ap-mddZl6dNqG5Y4yWHuiu0aX`) hit `torch.OutOfMemoryError` (DPLM-650M, batch 32, ~285-residue construct > 22 GiB) in the first training forward pass — data path was fine, GPU too small — so I changed the `train_generative_design` decorator to `gpu="H100"` (the proven config for `train_epistasis_e2e` on this image) and **relaunched** (`ap-BlbHQjQn930EN3cSt5zDpZ`, H100, writes `outputs/epistasis/gen_design.json`), verified healthy (1 active task, surviving the local client kill). Minimal launch iteration per protocol: RUNS.md + this file + `BLOCKERS.md` B1 updated (B1 → being resolved by computation; dissolves once the artifact lands); nothing else touched, tree green. **HARVEST next iteration:** `modal volume get phaseagent-data outputs/epistasis/gen_design.json .`, commit, fold a grounded generative-design subsection (reward-guided DPLM generation vs unconditioned DPLM / random / top-library ceiling) + a CLAIMS.md row + a no-drift guard test, recompile — completing DoD #4 and clearing the path to `ICML_READY` after the final-audit checklist.
- iter14 (2026-06-26): **Added the DoD #4 best-of-N rerank design comparator, then launched AND harvested the Model 2 run in one iteration — DoD #3 complete, DoD #4 best-of-N rerank grounded.** The Model 2 design experiment is a best-of-N reranking task (pick gain-of-function doubles from an additively-mediocre pool by a reward model), but the artifact only carried additive (pool mean) / Model 1 / oracle — the paper's §6.4-claimed best-of-N rerank baseline had no number. Extended `scripts/model2_design.py`: `build_oof_table` now carries a `pll_eps` column (the zero-shot DPLM PLL epistasis per double, the standard PLM-as-reward reward model), `summarize_model2` reports `gof_top10pct_zeroshot` + `gof_lift_zeroshot`, and `figure_model2` adds the zero-shot rerank curve to fig9 panel A. **Also fixed a latent no-drift fragility:** `run_model2` computed the summary from the in-memory table while the committed-artifact guard recomputes from the re-read CSV with exact `==`, and `topmean` (a literal mean, unlike rank-based Spearman) is not robust to a 1-ULP CSV serialization shift, so the real artifact's guard could have gone red on landing. Fixed by having `run_model2` summarize from the re-read CSV (the summary is now a bit-exact pure function of the committed CSV bytes, as CLAIMS.md asserts) and mirroring that in the synthetic test; added an assertion that Model 1's lift beats the (blind) zero-shot rerank lift. **Launched the Model 2 OOF run fully DETACHED** (`os.setsid`, PID 81468, PPID=1; `PYTHONPATH=src OMP_NUM_THREADS=8`) — it FINISHED within the same iteration (~9 min, the GB1 OOF is fast on CPU) and wrote all three artifacts, so I HARVESTED it now (the iter13 pattern: a run that lands mid-iteration is folded in immediately rather than left committed-but-unfolded). **Real Model 2 numbers (12,000 held-out GB1 doubles):** additive pool mean binding **−3.62**; Model 1 selection **−2.53 (lift +1.09)**; best-of-$N$ rerank by zero-shot DPLM PLL **−3.84 (lift −0.22 — worse than random; the PLM-as-reward baseline is blind to function epistasis)**; oracle **−2.00 (lift +1.62)**; matched-additive within-bin Spearman **0.28** over 10 bins. Folded into paper §7.4 (new grounded subsection + `tab:model2` + `fig:model2`) + the §4 method sentence; CLAIMS.md gained a GROUNDED Model 2 section + the fig9 row, and the Model 2 PENDING bullet was removed. The auto-activated `tests/test_model2_committed.py` passes on the real artifact. `pytest -q`: **116 passed / 4 skipped**. `tectonic` recompiles cleanly (exit 0, 7 pp, 0 undefined refs/cites, 0 `??` in the rendered PDF, no fbox/TODO/placeholder). **Remaining for ICML_READY: a final whole-DoD re-audit, and a human call on whether DoD #4's "one strong discrete-diffusion guidance baseline" is required given the paper makes a RANKING (not generative) claim — flagged in `BLOCKERS.md`.**
- iter13 (2026-06-26): **Harvested the DoD #2 headline AND folded in both Model 1 assays — two committed artifacts landed and were grounded this iteration.** (1) **DoD #2 (e2e headline, the chronic 11-iteration long-pole) finally landed.** `modal app list` showed `ap-ayiJz1iYjfF0M5qUt0Rhaj` `stopped`/0-tasks, but this time it COMPLETED: `e2e_results.json` was on the volume. Pulled + committed. Cross-protein generalization (H100, 10 epochs, lora_r=64, 12.9M trainable): trained on 124 proteins' doubles, evaluated on the **25 held-out proteins** → **median per-held-out-protein Spearman 0.335 (mean 0.329), 80% above ρ>0.2, vs zero-shot ref 0.25.** Folded into paper §7.5 (`tab:e2e`) + abstract + contributions + CLAIMS.md; guarded by `tests/test_e2e_artifact.py`. (2) **The iter11 detached Model 1 run finished GB1_Olson mid-iteration** (ALL_DONE 01:25:47) — `model1_oof_GB1_Olson.csv` + summary landed as untracked files. Re-ran the now-auto-activated `tests/test_model1_committed.py` on the REAL GB1 artifact (2 passed). Folded **both** Model 1 assays into a new paper §7.3 `tab:model1` (GB1 held-out-doubles **0.36** / position **0.12**; GFP **0.14** / **0.08**) and grounded the §7.2 function zero-shot floor with the real numbers (GB1 **0.021**, GFP **0.005**, from the committed `model1_*_summary.json:zeroshot_spearman`). CLAIMS.md: new GROUNDED Model 1 section; removed both now-grounded pending bullets. `tectonic` recompiles cleanly (exit 0, 6 pp, 0 undefined). `pytest -q`: 115 passed / 5 skipped. **Remaining for ICML_READY: launch+harvest the Model 2 OOF run (§7.4 `tab:model2`, the last DoD #3 piece) and the additive / best-of-N design baselines (DoD #4 design comparators).**
- iter1 (2026-06-25): Audited repo. Created PROGRESS.md + RUNS.md. Launched detached headline e2e run (train_epistasis_e2e, 10 epochs, lora_r=64, batch 128) — the cross-protein held-out number (DoD #2). See RUNS.md.
- iter2 (2026-06-25): The iter1 e2e run had DIED (stopped, 0 tasks, no logs) — diagnosed as killed mid-build of the COLD `gpu_lora_image` at the iter1 client teardown. Fix: pre-warm the image cache with `h100_probe` (now builds in ~3 s), then RE-LAUNCHED the headline e2e run detached (`ap-ayiJz1iYjfF0M5qUt0Rhaj`); verified it loads data (`120000 doubles, 149 proteins; holdout 20073 / 25 prot`), trains, and survives local client kill. Installed `tectonic` 0.16.9 locally (DoD #1 prereq). RUNS.md updated. DoD #2 artifact still pending the run's completion.
- iter3 (2026-06-26): Stood up the paper spine + grounded the decomposition section. (1) Committed the official ICML `icml2025` style files into `paper/` (verified tectonic compiles the upstream example cleanly). (2) `scripts/build_decomposition_artifact.py` reduces the committed atlas → committed `outputs/epistasis/decomposition_atlas.csv` + `decomposition_summary.json` (149 proteins, 131,062 doubles; median additive R² −0.91 → +global 0.72; specific std 0.42 kcal/mol, 28% var share); guarded by `tests/test_decomposition_artifact.py`. (3) Wrote `paper/epistasis_icml.tex` (all standard sections) + `paper/epistasis.bib` (12 real refs) — compiles cleanly with `tectonic` (5 pp, 0 undefined refs/cites, no \fbox/TODO/placeholder figs; cites only fig2/fig3 which regenerate from committed data). (4) Created `CLAIMS.md`. (5) `pytest -q`: 107 passed, 4 skipped. The e2e headline run (`ap-ayiJz1iYjfF0M5qUt0Rhaj`) is still RUNNING — artifact pending.
- iter5 (2026-06-26): Pulled the **zero-shot DPLM PLL stability baseline** off the volume (`run_headroom_gate` done) and committed `outputs/epistasis/headroom_{summary.json,dplm.parquet}`. Folded its real numbers (30 proteins, 15,000 doubles; median per-protein Spearman 0.25, overall 0.23, 60% above ρ>0.2) into paper §7.2 as a new grounded subsection + `tab:zeroshot`, replacing the forward-reference; verified the per-protein stats independently reproduce from the parquet. Updated CLAIMS.md (new GROUNDED zero-shot-stability section; trimmed the matching PENDING row), RUNS.md (headroom → DONE; e2e re-polled, still 1 task, `e2e_results.json` not yet on volume — left baking), and fixed a related-work cross-ref. `tectonic` recompiles cleanly (6 pp, exit 0, 0 undefined refs/cites, no fbox/TODO/placeholder). No code touched, so `pytest` unchanged from iter3 (107 passed / 4 skipped). DoD #4 stability-baseline half now grounded; function half + e2e still pending their runs.
- iter4 (2026-06-26): Polled the e2e headline run — it RESTARTED but is healthy and actively training (~9.5 min/epoch, epoch 2/10 at poll time; see RUNS.md), `e2e_results.json` not yet on volume, left baking. Then (minimal launch iteration) launched the **zero-shot DPLM PLL baseline** `run_headroom_gate` (`ap-a7hQxHVfIOYVuoil7e6Go7`, A10G, detached) so DoD #4's zero-shot baseline bakes in parallel with the headline run. Verified it past the image build into GPU scoring (`built 15000 doubles; skipped 0`, `scoring 20142 unique sequences`) before ending the iteration. RUNS.md + CLAIMS.md updated; no code touched, tree green.
- iter8 (2026-06-26): Confirmed **both `feat_{GB1_Olson,GFP}.npz` have landed on the volume** (all four function `.npz` now present) — DoD #3 Model 1 is UNBLOCKED. Pulled the matched `feat`+`shift` pairs to `/tmp`, measured the **function-side zero-shot DPLM PLL Spearman directly: GFP 0.005, GB1_Olson 0.021** (≈0, the DoD #4 function-half contrast with stability 0.25; NOT paper-eligible until written to a committed artifact). Started the pure-CPU `train_model1.py` grounding run (200 epochs, both assays); a forced iteration-conclusion caught it mid-GFP, so no Model 1 CSV committed this iteration. Landed the committable, verified increment instead: (1) `tests/test_model1_committed.py` — a skip-until-present guard that, once `model1_oof_<assay>.csv` + `model1_<assay>_summary.json` are committed, asserts the summary reproduces from the OOF CSV (no drift) and that Model 1 beats the near-zero zero-shot floor on held-out doubles for both assays; (2) RUNS.md (both feat runs → DONE; documented the CPU-only Model 1 grounding recipe + that `train_model1.py` writes only at the very end so a mid-run kill is clean); (3) this file + CLAIMS.md. Tree green: only the new test added, no partial artifacts, `pytest tests/test_model1_*` = 1 passed / 2 skipped. **Next iteration: run `train_model1.py` to completion for both assays (~9 min each, no GPU), commit the OOF CSVs + summaries, and fold the Model 1 doubles/position + function zero-shot numbers into a new paper §7.4 table + CLAIMS.md.** e2e re-polled, still baking.
- iter12 (2026-06-26): **Did the Model 2 gating dry-work unit (the iter7-equivalent, for Model 2).** Refactored `scripts/model2_design.py`: the previous file ran a ~9-min KFold OOF job at IMPORT time and only PRINTed numbers + saved a figure (so Model 2 was un-importable, un-testable, and ungroundable). Split into `summarize_model2(df)` (pure function deriving the paper numbers from the 4-column per-double table), `build_oof_table` / `run_model2` (writes the committed `model2_oof_GB1.csv` + `model2_GB1_summary.json`), `figure_model2(csv)` (regenerates fig9 from the committed CSV, no retraining), and a CLI `main`. Added `tests/test_model2_committed.py`: a synthetic drift-guard on `summarize_model2` (round-trips through CSV, asserts the summary recomputes exactly + the gof lift / matched-additive Spearman are positive) plus a skip-until-present committed-artifact guard. `pytest -q`: 111 passed, 7 skipped (tree green). Did NOT run the 9-min Model 2 OOF this iteration (CPU is busy with the detached Model 1 run; running both contends). **The iter11 detached Model 1 run landed GFP mid-iteration:** `outputs/epistasis/model1_oof_GFP.csv` (828 KB) + `model1_GFP_summary.json` are now on disk and committed this iteration — real, guarded numbers (zero-shot 0.005, held-out-DOUBLES Spearman **0.138** (+0.133), held-out-POSITION **0.079** (+0.074)); `tests/test_model1_committed.py` auto-activated for GFP and PASSES (full suite 113 passed / 5 skipped). The detached run is now training GB1_Olson (still baking). **NEXT ITERATION: harvest GB1 (`model1_oof_GB1_Olson.csv` + summary), then fold BOTH Model 1 assays into one new paper §7.3 `tab:model1` + the §7.2 function zero-shot sentence + CLAIMS.md (one clean edit, both assays, recompile once).** NOT folded into the paper this iteration to avoid a half-filled table / double edit. e2e (`ap-ayiJz1iYjfF0M5qUt0Rhaj`) re-polled: still 1 task, `e2e_results.json` not on volume.
- iter11 (2026-06-26): **Fixed the chronic iter9/iter10 Model 1 loss.** Root cause: the pure-CPU `train_model1.py` run was launched as a harness-tracked `Bash run_in_background`, which the `structured-output-enforce` forced-conclusion KILLS at the iteration boundary (exit 144) before the ~18-min two-assay run writes any artifact. Fix: RE-LAUNCHED it **fully detached** via a Python `os.fork()`+`os.setsid()` wrapper (macOS has no `setsid` binary) — verified orphaned (PID 63070, PPID=1, STAT `Ss`, new session), so it now survives the boundary like a Modal run and is harvested next iteration. Also pre-staged `/tmp/SPG1_STRSG_Olson_2014.csv` (246 MB) for the follow-on Model 2 step, and documented the exact HARVEST recipe in RUNS.md (commit the 4 artifacts → §7.3 `tab:model1` + §7.2 function zero-shot + CLAIMS.md → `pytest -q` → recompile). Tree green (only RUNS.md + this file changed; no partial artifacts; guard `tests/test_model1_committed.py` still skips). e2e (`ap-ayiJz1iYjfF0M5qUt0Rhaj`) re-polled: still 1 task, `e2e_results.json` not yet on volume, left baking.
- iter6 (2026-06-26): Polled e2e (`ap-ayiJz1iYjfF0M5qUt0Rhaj`) — still RUNNING, restarted again (currently epoch 3/10 in its live log), `e2e_results.json` not yet on volume, left baking. Then (minimal launch iteration) launched the **entire FUNCTION feature-extraction pipeline** — the gate for DoD #3 (Model 1 + Model 2) and the DoD #4 function-side zero-shot, which had NEVER been launched: 4 detached A10G runs `extract_function_{features,shifts}` for GB1_Olson + GFP (app ids in RUNS.md). Verified all 4 healthy past the image build into the function body (DPLM weights loading, 1 active task each); both `shift` runs already FINISHED (`shift_GB1_Olson.npz`, `shift_GFP.npz` on volume), both `feat` runs still scoring. RUNS.md (4 new rows + artifact-chain note), PROGRESS.md, CLAIMS.md updated; no code touched, `pytest` unchanged (107 passed / 4 skipped). Tree green.
